# v0.7.1-beta.7 更新内容

## 格子里的地点不再被白白截掉

上一版把地点硬压成一行，结果跨大节的高格子下面明明空着一大片，地点却只显示「@东…」。现在课名占完之后剩多少高度，地点就用多少——放得下就整段显示，真放不下才省略号收尾。

课名仍然优先，不会被地点挤掉。

## 导课页不再自相矛盾

搜索时上面写「已加载 1 个插件」、下面写「搜到 0 个」，看着像自相矛盾——前者说的是清单总数，后者说的是命中数。现在合成一句「在 N 个插件里搜到 M 个」。

搜不到时也不再只留一个空屏：清单里已有的插件照样列在下面，不至于明明有插件却找不到入口。
