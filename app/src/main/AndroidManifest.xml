<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Android 16 实时活动：上课通知在状态栏挂胶囊、锁屏置顶 -->
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />
    <!--
        只给「上课通知 - 悬浮窗增强」这一档皮肤用：动效和毛玻璃在通知 API 里没有，
        要这两样就只能自己加一个窗口来画。默认皮肤是系统原生样式，用不到这条权限，
        所以不主动申请，用户选了那一档才去引导授权。
    -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!--
        Android 14 起 SCHEDULE_EXACT_ALARM 默认不授予，闹钟会在排程前就被挡下。
        USE_EXACT_ALARM 安装即授予，适用于以闹钟为核心功能的应用；
        旧系统上仍用 SCHEDULE_EXACT_ALARM，那一档装了就自动给。
    -->
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <!-- MIUI 13 以下要靠这条权限才弹得出「添加到桌面」确认框，AOSP 上没有实际作用 -->
    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <!--
        Android 11 起的包可见性：不声明这段就查不到桌面应用是谁，
        厂商识别与「找不到小组件」的自检都会给出错误结论。
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.HOME" />
        </intent>
        <!--
            各家的自启动、省电策略设置页。Android 11 起不声明就查不到这些应用，
            权限页会以为厂商页面不存在，自启动那一项就不显示。
        -->
        <package android:name="com.miui.securitycenter" />
        <package android:name="com.miui.powerkeeper" />
        <package android:name="com.huawei.systemmanager" />
        <package android:name="com.hihonor.systemmanager" />
        <package android:name="com.coloros.safecenter" />
        <package android:name="com.oplus.battery" />
        <package android:name="com.oppo.safe" />
        <package android:name="com.vivo.permissionmanager" />
        <package android:name="com.vivo.abe" />
        <package android:name="com.iqoo.secure" />
        <package android:name="com.oneplus.security" />
        <package android:name="com.samsung.android.lool" />
        <package android:name="com.meizu.safe" />
    </queries>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:name=".app.ClassScheduleApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.CurSimple"
        android:enableOnBackInvokedCallback="true"
        android:localeConfig="@xml/locales_config">
        <!-- Vendor opt-ins so MIUI / vivo / HONOR launchers index our widgets in their pickers. -->
        <meta-data android:name="miuiWidgetVersion" android:value="1" />
        <meta-data android:name="vivoWidgetVersion" android:value="1" />
        <!--
          允许华为 / 荣耀的系统服务（智慧助手、系统日历）读取本应用的数据。
          这是华为侧「让课程出现在系统卡片里」的开关：不声明时系统服务不会来读。
          只是一个许可位，本身不主动上传任何东西。
        -->
        <meta-data android:name="sync_data_to_system_service" android:value="true" />
        <activity
            android:name=".app.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".app.reminder.AlarmRingingActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTop"
            android:showWhenLocked="true"
            android:theme="@style/Theme.CurSimple.AlarmRinging"
            android:turnScreenOn="true" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <provider
            android:name=".app.provider.PrivateFilesDocumentsProvider"
            android:authorities="${applicationId}.privatefiles.documents"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

        <receiver
            android:name=".app.reminder.SystemAlarmEnvironmentReceiver"
            android:directBootAware="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.DATE_CHANGED" />
                <action android:name="android.intent.action.LOCALE_CHANGED" />
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".app.reminder.SystemAlarmCheckReceiver"
            android:exported="false" />

        <receiver
            android:name=".app.holiday.HolidayEveMuteReceiver"
            android:exported="false" />

        <receiver
            android:name=".app.reminder.AppAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.x500x.cursimple.action.APP_ALARM_TRIGGER" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".app.reminder.AutoSilenceReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="com.x500x.cursimple.action.AUTO_SILENCE_BOUNDARY" />
                <action android:name="com.x500x.cursimple.action.AUTO_SILENCE_RESTORE_NOW" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".app.reminder.AlarmKeepAliveRestartReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.x500x.cursimple.action.KEEP_ALIVE_RESTART" />
                <action android:name="com.x500x.cursimple.action.REMINDER_WATCHDOG" />
            </intent-filter>
        </receiver>

        <service
            android:name=".app.reminder.AlarmKeepAliveService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps user scheduled course alarms registered after the app is swiped away" />
        </service>
        <!-- 提醒巡检：系统每 15 分钟拉起一次，重挂上课提醒、拉回守护服务 -->
        <service
            android:name=".app.reminder.ReminderGuardJobService"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <service
            android:name=".app.reminder.AlarmRingingService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback|specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User scheduled course alarms ring when the app process is not already open" />
        </service>
        <receiver
            android:name=".app.notice.ClassNoticeReceiver"
            android:exported="false" />
        <receiver
            android:name=".app.notice.AlarmPreNoticeReceiver"
            android:exported="false" />

    </application>

</manifest>
